/* const request = require('supertest');
const { expect } = require('chai');
const app = require('../app');// O el archivo que exporta tu servidor Express

describe('GET /reservas', () => {
  it('debería retornar 200 y una lista de reservas', async () => {
    const res = await request(app).get('/reservas');

    expect(res.status).to.equal(200);
    expect(res.body).to.be.an('array'); // Ajusta esto si tu respuesta es diferente
  });
}); */
